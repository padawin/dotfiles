-- [[ Setting options ]]
-- See `:help vim.opt`

-- Preview substitutions live, as you type!
-- Seems to work already without the option
--vim.opt.inccommand = 'split'

-- Don't show the mode, since it's already in the status line
--vim.opt.showmode = false
