https://github.com/junegunn/fzf
https://github.com/rwos/gti

rxvt-unicode
i3blocks
flameshot

To disable capslocks, the following line has to be changed in
`/etc/default/keyboard`:

	XKBOPTIONS="ctrl:nocaps"

For italic terminal:
tic -x xterm-256color-italic.terminfo

To set Gnome Terminal up, run:

	dconf load /org/gnome/terminal/ < ./gnome-terminal.config

To save Gnome Terminal, run:

	dconf dump /org/gnome/terminal/ > ./gnome-terminal.config
